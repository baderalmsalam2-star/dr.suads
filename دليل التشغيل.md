# دليل التشغيل — منصة التدريس

دليل عملي خطوة بخطوة. لا يحتاج خبرة برمجية.

---

## ١) كيف أفتح التطبيق؟ (أسهل طريقة — بلا تنصيب شيء)

التطبيق صفحات ثابتة، يشتغل من القرص مباشرةً بلا خادم ولا إنترنت.

1. افتح صفحة المستودع على GitHub:
   `https://github.com/baderalmsalam2-star/dr.suads`
2. تأكد أن الفرع المختار أعلى اليسار هو `main`.
3. اضغط الزر الأخضر **Code** ← **Download ZIP**.
4. فُكّ الضغط عن الملف المنزَّل (كليك يمين ← Extract All / استخراج).
5. ادخل المجلد، ثم **اضغط ضغطتين على `index.html`**.

انفتحت المنصة في المتصفح. هذا كل شيء.

> **مهم:** استعمل نفس المتصفح ونفس المجلد في كل مرة. البيانات (كشف
> الطالبات، نقاط التفاعل، التسليمات) محفوظة داخل المتصفح نفسه، فلو فتحته
> بمتصفح ثانٍ أو من مجلد ثانٍ ستجده فارغًا.

### طريقة ثانية: خادم محلي

بعض الأمور تكون أنظف مع خادم محلي (وهي ضرورية لو منع متصفحك ملفات
`file://`). افتح الطرفية داخل مجلد المشروع ونفّذ:

**ماك أو لينكس:**
```bash
cd ~/Downloads/dr.suads-main
python3 -m http.server 8000
```

**ويندوز (PowerShell):**
```powershell
cd $HOME\Downloads\dr.suads-main
python -m http.server 8000
```

ثم افتح المتصفح على: **http://localhost:8000**

لإيقاف الخادم: اضغط `Ctrl + C` في نافذة الطرفية.

> لو ما كان بايثون منصَّبًا وعندك Node:
> `npx serve .` ثم افتح الرابط الذي يطبعه.

### فتحه على الجوال أو جهاز الطالبات

`localhost` يعمل على جهازك وحده. لعرضه على أجهزة أخرى في نفس شبكة الواي‑فاي:

1. اعرف عنوان جهازك في الشبكة:
   ماك: `ipconfig getifaddr en0` — ويندوز: `ipconfig` (خانة IPv4).
2. شغّل الخادم كما سبق.
3. من الجوال افتح `http://<العنوان>:8000` مثلًا `http://192.168.1.14:8000`.

انتبه: كل جهاز له تخزينه الخاص — لا تتزامن البيانات بينها. راجع
«أين تُحفظ البيانات» في `README.md`.

---

## ٢) تسجيل الطالبات بالباركود (أول محاضرة)

باركود واحد تعرضينه على الشاشة، كل طالبة تمسحه فتفتح لها صفحة تسجيل،
ثم تعرض عليكِ رمزها فتدخل الكشف.

### قبل المحاضرة: شغّلي المنصة على الشبكة

الباركود يحمل عنوانًا لازم يفتح من جوال الطالبة، فلا يكفي فتح الملف
من القرص. خطوتان:

1. **اعرفي عنوان جهازك في الشبكة:**
   - ماك: `ipconfig getifaddr en0`
   - ويندوز: `ipconfig` ثم انظري خانة IPv4
2. **شغّلي الخادم** داخل مجلد المشروع:
   ```bash
   python3 -m http.server 8000      # ويندوز: python -m http.server 8000
   ```

تأكدي أن جهازك وأجهزة الطالبات على **نفس شبكة الواي‑فاي**.

### في المحاضرة

1. افتحي **الباركود** من الشريط العلوي، واختاري الشعبة.
2. أول مرة فقط: اكتبي عنوان المنصة (مثل `http://192.168.1.14:8000`)
   واضغطي **حفظ** — يُحفظ فلا تعيدي كتابته.
3. اضغطي **ملء الشاشة** واعرضيه على البروجكتر.
4. الطالبة تمسحه بكاميرا جوالها → تكتب اسمها الكامل ورقمها الجامعي →
   **تسجيل** → يظهر لها رمز خاص بها.
5. افتحي **الطالبات ← استقبال التسجيل**، واستقبلي الرموز بأي من الثلاث:

| الطريقة | متى تُستعمل |
|---|---|
| **مسح بالكاميرا** | الأسرع. تُوجَّه كاميرا اللابتوب إلى جوال الطالبة فتُضاف فورًا. يحتاج متصفح Chrome أو Edge. |
| **لصق الرموز** | الطالبة ترسل سطر رمزها (واتساب/تيمز)، وتلصقين عدة أسطر دفعة واحدة. يعمل في كل المتصفحات. |
| **استلام ملفات** | الطالبة تنزّل ملف تسجيلها وترسله، وتختارين الملفات دفعة واحدة. |

الطالبة المسجَّلة مسبقًا تُكتشف برقمها الجامعي فلا تتكرر. والرموز غير
الصالحة تُرفض ويُذكر عددها.

> الطالبة تحتفظ بصفحتها على جوالها من زر **صفحتي**، فترى نقاطها
> وأوراقها. والبيانات محفوظة على جهازها لا على خادم.

---

## ٣) تجهيز الشعبة يدويًا (بديل الباركود)

1. افتح **الطالبات** من الشريط العلوي.
2. اختر الشعبة.
3. اضغط **لصق كشف الأسماء**، والصق الأسماء **اسمًا في كل سطر** بترتيب الكشف.
4. **حفظ الكشف**.

ترتيب الأسماء هو نفسه أرقام القارئات في المحاضرات، ولسان القارئة في العرض
سيعرض الاسم بدل الرقم.

> الأسماء المكتوب أمامها «اسم مبدئي» مولَّدة تلقائيًا — استبدلها بالكشف الحقيقي.

**خذ نسخة احتياطية بعد كل محاضرة:** الطالبات ← **نسخة احتياطية** (ينزل ملف
`tp-backup-…json`). استعادته من زر **استعادة** — يدمج ولا يمسح.

---

## ٤) الحضور والغياب

### مرة واحدة في الفصل: جدول التواريخ

**الحضور ← جدول التواريخ**، حدّدي تاريخ أول محاضرة وأيامها في الأسبوع،
واضغطي **توليد** — تُولَّد تواريخ المحاضرات الثلاثين. أي تاريخ يُعدَّل بعدها
من الجدول نفسه (مثل تأجيل محاضرة لعطلة).

### في كل محاضرة

1. افتحي **الحضور** — تُفتح محاضرة اليوم تلقائيًا بحسب الجدول.
2. اضغطي **الكل حاضرات**، ثم علّمي الغائبات فقط. أسرع من تعليم الكل واحدة واحدة.
3. الحالات الأربع: **ح** حاضرة · **ت** متأخرة · **ع** بعذر · **غ** غائبة.

يُحفظ فورًا بلا زر حفظ، ويُعاد التعليم متى شئتِ.

### التقرير والحرمان

أسفل الصفحة تقرير بكل طالبة: عدد كل حالة ونسبة غيابها بشريط ملوّن.
من قاربت الحد تُعلَّم **«قاربت الحد»**، ومن تجاوزته **«تجاوزت حد الحرمان»**.

> **«بعذر» لا تُحتسب غيابًا ولا تدخل في المقام** — أي تُستثنى المحاضرة من
> حساب نسبة الطالبة كأنها لم تُعقد في حقها.

حد الحرمان الافتراضي **٢٥٪** والتنبيه عند **١٥٪**، ويُعدَّلان في
`data/course.js` تحت `attendance` بحسب لائحة الكلية — تأكدي من الرقم
المعتمد عندكم فهو يختلف بين الكليات.

**تصدير:** زر **تصدير** ينزّل ملف CSV يُفتح في Excel بالعربية صحيحة،
فيه كل محاضرة عمودًا وأيام الغياب ونسبتها — جاهز لتسليمه للقسم.

## ٥) أثناء المحاضرة

1. من **الرئيسية** اختر الشعبة، ثم اضغط بطاقة المحاضرة.
2. تنقّل بـ `←` و `→`. `مسافة` تُظهر جواب السؤال. `ر` يعيد المؤقت.
3. لرصد التفاعل: اضغط **`م`** (أو زر «رصد» أسفل الشاشة).
   - اضغط اسم الطالبة ← ثم نوع التفاعل. تُحفظ النقطة فورًا.
   - **تراجع** يلغي آخر رصد لتلك الطالبة.
   - القارئة المسنَدة للشريحة الحالية محدَّدة بإطار أصفر.
4. في نهاية اليوم: **لوحة الشرف** ← المدى **اليوم** لعرض الأوائل الثلاث.
   وللشهر أو الـ midterm: بدّل المدى إلى **الشهر**.

---

## ٦) أوراق العمل والواجبات

**للدكتورة:** أوراق العمل ← اضغط الورقة لمعاينتها، والمصفوفة أسفل الصفحة
تبيّن من سلَّم ومن لم يسلّم.

**دورة التسليم الحالية (بلا خادم):**

1. الطالبة تفتح الورقة على جهازها وتحلها، ثم **تسليم**.
2. تضغط **تنزيل ملف التسليم** — ينزل ملف مثل
   `tasleem-sec1-no3-w1-2026-09-05.json`.
3. ترسله بالبريد أو تيمز أو واتساب.
4. الدكتورة: أوراق العمل ← **استلام ملف تسليم** ← تختار الملفات
   (عدة ملفات دفعة واحدة). يُطابَق التسليم على صاحبته، ويُحدَّث لا يُكرَّر،
   وتُرصد نقطة التسليم تلقائيًا.

---

## ٧) إضافة محاضرة جديدة — الطريقة السهلة (مُنشئ المحاضرات)

افتح **مُنشئ المحاضرات** من الشريط العلوي:

1. اكتب رقم المحاضرة وعنوانها وصفحاتها.
2. **الصق نص المحاضرة من المذكرة** — افصل كل فقرة عن التي بعدها بسطر فارغ —
   ثم اضغط **قسّم إلى شرائح**.
3. لكل شريحة اختر نوعها: *قراءة* (تُسنَد لطالبة) أو *تقرؤها الدكتورة*،
   واكتب لها عنوانًا جانبيًا ورقم صفحتها.
   - لتلوين آية أو نص مقتبس بالأزرق ضعه بين قوسين مزدوجين: `((النص))`
4. **أضف سؤالًا** لكل سؤال: نصه، خياراته، علّم الصحيح، واكتب تعليق الجواب.
5. **أضف شريحة ختام** واكتب الخلاصة وعنوان المحاضرة القادمة.
6. **ابنِ المحاضرة** — تظهر معاينة حيّة كما ستراها الطالبات.
7. **نزّل ملف المحاضرة** واحفظه داخل مجلد `sessions/`.
8. انسخ السطر الظاهر أسفل الصفحة والصقه في `data/course.js` داخل
   `sessions`. (وإن أردت ورقة عمل من أسئلة المحاضرة، الصق السطر الثاني في
   `data/worksheets.js`.)

حدِّث الصفحة الرئيسية — تظهر المحاضرة «متاحة».

> الأداة لا تكتب محتوى من عندها: كل كلمة في المحاضرة الناتجة جاءت من النص
> الذي لصقته.

## ٧ب) إضافة محاضرة يدويًا (بتحرير الملفات)

1. انسخ `sessions/_template.html` وسمِّه مثلًا `sessions/wilaya/03-اسم-المحاضرة.html`.
2. غيّر `<title>` و `data-session="3"` في وسم `<body>`.
3. استبدل أقسام `<section class="slide">` بمحتوى المحاضرة.
4. افتح `data/course.js` وأضف المحاضرة إلى `sessions`:

```js
{
  n: 3,
  title: "عنوان المحاضرة",
  subtitle: "سطر تعريفي",
  pages: "ص ٥–٦",
  file: "sessions/03-اسم-المحاضرة.html",
  readers: 5,          // عدد شرائح data-reader في الملف
  status: "ready"
}
```

`readers` مهم: منه تُحسب بداية قارئات المحاضرة التالية.

### وسوم الشرائح

| الوسم | أثره |
|---|---|
| `data-tab="…"` | نص اللسان الأصفر أعلى الإطار |
| `data-src="…"` | المصدر أو رقم الصفحة في الشريط العلوي |
| `data-reader` | شريحة قراءة — تُسنَد إلى طالبة تلقائيًا |
| `data-timer="30"` | مؤقت بالثواني |
| `data-q` | شريحة سؤال — `<li class="right">` هو الصواب |

---

## ٨) إضافة ورقة عمل أو واجب

في `data/worksheets.js` أضف عنصرًا:

```js
{
  id: "w3",                      // معرّف فريد
  type: "worksheet",             // أو "homework" للواجب
  session: 3,
  title: "ورقة عمل: العنوان",
  pages: "ص ٥–٦",
  intro: "سطر تمهيدي.",
  items: [
    { id:"q1", kind:"mcq", prompt:"السؤال؟",
      options:["أ","ب","ج","د"], answer:1, why:"تعليق بعد التسليم." },
    { id:"q2", kind:"essay", rows:5, prompt:"سؤال مقالي." },
    { id:"q3", kind:"text",  prompt:"إجابة بسطر واحد." },
    { id:"q4", kind:"file",  prompt:"أرفقي ملفًا.", optional:true }
  ]
}
```

`answer` هو **فهرس** الخيار الصحيح (يبدأ من صفر: ٠ = أ، ١ = ب …).

---

## ٩) تغيير نقاط التفاعل

في `data/course.js` داخل `engagement.kinds` — عدّل الأرقام كما تشاء.

---

## ١٠) مشاكل شائعة

| العرض | السبب والحل |
|---|---|
| الصفحة فاضية والخط غريب | لا يوجد إنترنت فسقط خط Amiri إلى خط النظام. التطبيق يعمل، الشكل فقط يختلف. |
| اختفى كشف الطالبات | فتحته من متصفح أو مجلد مختلف، أو مُسحت بيانات المتصفح. استعِد النسخة الاحتياطية. |
| ملفات التسليم كلها باسم `download` | نسخة قديمة. اسحب آخر تحديث. |
| رفع الملفات لا يعمل | تصفُّح خاص (Private/Incognito) يمنع التخزين. افتحه بنافذة عادية. |
| لوحة الرصد لا تفتح بـ `م` | لوحة المفاتيح بالإنجليزية — استعمل `m` أو زر «رصد». |
| الباركود يقول «لم يُحدَّد عنوان المنصة» | افتحي المنصة عبر خادم محلي، أو اكتبي العنوان في حقل صفحة الباركود. |
| جوال الطالبة لا يفتح الرابط | الجهازان ليسا على نفس الواي‑فاي، أو الجدار الناري يمنع المنفذ ٨٠٠٠. |
| «الكاميرا غير مدعومة» | المتصفح ليس Chrome/Edge. استعملي اللصق أو الملفات. |

---

تم تطوير التطبيق بواسطة بدر المسلم
